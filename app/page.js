"use client";import{useEffect,useState}from"react";
export default function Home(){const[url,setUrl]=useState("");const[job,setJob]=useState(null);const[message,setMessage]=useState("");const[loading,setLoading]=useState(false);
useEffect(()=>{const id=localStorage.getItem("clipFinderJob");if(id)loadJob(id)},[]);
async function loadJob(id){try{const r=await fetch("/api/jobs/"+id,{cache:"no-store"}),d=await r.json();if(r.ok&&d.job){setJob(d.job);setMessage(`${d.job.stage} • ${d.job.progress}%`)}}catch{}}
async function analisar(e){e.preventDefault();if(!url.trim())return;setLoading(true);setMessage("Criando análise...");try{const r=await fetch("/api/jobs",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({url})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Não foi possível criar a análise.");localStorage.setItem("clipFinderJob",d.job.id);setJob(d.job);setMessage("Job criado e salvo • 0%");}catch(e){setMessage("Erro: "+e.message)}finally{setLoading(false)}}
return <main><section className="hero"><div className="tag">CLIP FINDER • JOBS</div><h1>Encontre cortes.<br/>Poste mais rápido.</h1><p>Agora cada análise fica registrada para continuar evoluindo sem depender de uma única conexão do celular.</p><form onSubmit={analisar}><input value={url} onChange={e=>setUrl(e.target.value)} placeholder="Cole o link do VOD da Kick"/><button disabled={loading}>{loading?"Criando...":"Criar análise"}</button></form>{message&&<div className="status">{message}</div>}{job&&<div className="steps"><b>📡 Análise salva</b><span><strong>Status:</strong> {job.status}</span><span><strong>Etapa:</strong> {job.stage}</span><span><strong>Progresso:</strong> {job.progress}%</span><span><strong>ID:</strong> {job.id}</span><button type="button" onClick={()=>loadJob(job.id)}>Atualizar status</button></div>}</section></main>}