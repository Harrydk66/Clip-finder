const KICK_HOSTS=new Set(["kick.com","www.kick.com"]);function parseVod(raw){try{const u=new URL(raw);if(!KICK_HOSTS.has(u.hostname))return null;const m=u.pathname.match(/^\/([^/]+)\/videos\/([A-Za-z0-9-]+)\/?$/);if(!m)return null;return{creator:m[1],videoId:m[2],url:u.toString()}}catch{return null}}
export async function POST(req){const body=await req.json().catch(()=>({}));const vod=parseVod(body.url||"");if(!vod)return Response.json({ok:false,error:"Cole um link válido de VOD da Kick."},{status:400});return Response.json({ok:true,vod,status:"queued",message:"VOD validado. Pronto para a etapa de extração e análise."})}