function cfg(){const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)throw new Error("Supabase ainda não configurado.");return{url,key}}
async function call(path,options={}){const{url,key}=cfg();const r=await fetch(url+"/rest/v1/"+path,{...options,headers:{apikey:key,Authorization:"Bearer "+key,"Content-Type":"application/json",Prefer:options.prefer||"return=representation",...(options.headers||{})},cache:"no-store"});const text=await r.text();let data=null;try{data=text?JSON.parse(text):null}catch{}if(!r.ok)throw new Error(data?.message||text||"Erro no Supabase");return data}
export async function createJob(row){const d=await call("analysis_jobs",{method:"POST",body:JSON.stringify(row)});return d?.[0]}
export async function getJob(id){const d=await call("analysis_jobs?id=eq."+encodeURIComponent(id)+"&select=*",{method:"GET"});return d?.[0]||null}
export async function patchJob(id,patch){const d=await call("analysis_jobs?id=eq."+encodeURIComponent(id),{method:"PATCH",body:JSON.stringify({...patch,updated_at:new Date().toISOString()})});return d?.[0]||null}
